# pwa_installer.js

Вот готовый файл `README.md`. Он структурирован, красиво оформлен и содержит всю необходимую информацию для других разработчиков.

Вы можете просто создать файл `README.md` в корне репозитория и вставить туда этот текст.

***

```markdown
# 📱 PWA Install Modal

Легковесный JavaScript модуль для создания красивого окна установки PWA (Progressive Web App). 
Работает на Android (Chrome, Samsung) и iOS (Safari), автоматически подстраивается под тему устройства и не требует внешних зависимостей.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Size](https://img.shields.io/badge/size-<5kb-green.svg)

## ✨ Особенности

- 🎨 **Нативный дизайн**: Выглядит как часть системы (стилистика Apple/Android).
- 🌓 **Темная и Светлая темы**: Автоматическое определение `prefers-color-scheme` или принудительная настройка.
- 🍏 **Поддержка iOS**: Показывает специальную инструкцию для iPhone/iPad (так как там нет события установки).
- 🤖 **Поддержка Android**: Перехватывает событие `beforeinstallprompt` и вызывает системный диалог.
- 🔗 **Гибкая привязка**: Может создавать свою плавающую кнопку или привязываться к любому элементу на вашем сайте по ID.
- ⚡ **Zero Dependencies**: Чистый JS, SVG иконки встроены внутрь скрипта.

---

## 🚀 Быстрый старт

### 1. Подключение
Скачайте файл `pwa_installer.js` и подключите его перед закрывающим тегом `</body>`.

### 2. Вариант A: Автоматическая кнопка
Если вы хотите, чтобы скрипт сам создал плавающую кнопку "Установить" в углу экрана:

```html
<script 
    src="pwa_installer.js"
    data-app-name="My Awesome App"
    data-app-icon="/assets/logo.png"
></script>
```

### 3. Вариант B: Своя кнопка в меню
Если у вас уже есть кнопка в верстке (например, в хедере), и вы хотите вызывать окно по нажатию на неё:

```html
<!-- Ваша кнопка в меню -->
<button id="install-trigger">Скачать приложение</button>

<!-- Скрипт с привязкой к ID -->
<script 
    src="pwa_installer.js"
    data-trigger-id="install-trigger"
    data-app-name="My App Name"
    data-app-icon="/logo.png"
></script>
```

---

## ⚙️ Параметры (API)

Все настройки передаются через `data-` атрибуты тега `<script>`. Ни один параметр не является обязательным (будут использованы дефолтные значения).

| Атрибут | Значение по умолчанию | Описание |
| :--- | :--- | :--- |
| `data-trigger-id` | `null` | ID элемента, по клику на который откроется модалка. Если не указан, создается плавающая кнопка. |
| `data-app-name` | `Web App` | Название приложения в модальном окне. |
| `data-app-icon` | `/favicon.ico` | Путь к иконке приложения (желательно квадратная, PNG/JPG). |
| `data-app-color` | `auto` | Тема оформления. Варианты: `auto` (системная), `light`, `dark`. |
| `data-app-btn-position` | `left` | Позиция плавающей кнопки. Варианты: `left`, `right`. |
| `data-app-btn-text` | `Install App` | Текст на маленькой плавающей кнопке. |
| `data-app-title` | `Install App` | Заголовок модального окна. |
| `data-app-install-text` | `Install App` | Текст на большой кнопке действия внутри окна. |
| `data-app-ios-test` | `Add to Home Screen` | Текст кнопки для iOS (так как там нельзя установить кликом). |
| `data-ios-subtitle` | `Tap the Share button...` | Текст подсказки-инструкции для iOS. |

---

## 🎨 Примеры конфигурации

### Темная тема + Кнопка справа
```html
<script 
    src="pwa-installer.js"
    data-app-name="Crypto Wallet"
    data-app-icon="/icon.png"
    data-app-color="dark"
    data-app-btn-position="right"
    data-app-install-text="Установить сейчас"
></script>
```

### Полная локализация на Русский
```html
<script 
    src="pwa-installer.js"
    data-app-name="Мой Магазин"
    data-app-icon="/logo.png"
    data-app-title="Установка приложения"
    data-app-install-text="Скачать на устройство"
    data-app-ios-test="Добавить на экран Домой"
    data-ios-subtitle="Нажмите 'Поделиться' и выберите 'На экран Домой'"
></script>
```

---

## ⚠️ Важные требования

Чтобы кнопка установки работала (особенно на Android), ваш сайт должен соответствовать требованиям PWA:

1. **HTTPS**: Сайт должен работать по защищенному протоколу.
2. **Manifest.json**: В корне сайта должен быть корректный манифест.
3. **Service Worker**: Должен быть зарегистрирован Service Worker (даже простой).

Если эти условия не выполнены, браузер не отправит событие `beforeinstallprompt`, и на Android модальное окно может не открыться (или откроется, но нажатие на кнопку ничего не сделает).

---

## 📱 Как это работает?

1. **Android / Desktop**: Скрипт "слушает" событие браузера о возможности установки. Если оно приходит, он сохраняет его. При нажатии пользователем на кнопку в модалке, скрипт вызывает системный промпт.
2. **iOS**: Так как Safari не поддерживает программный вызов установки, скрипт определяет устройство по User Agent и показывает красивую инструкцию, куда нужно нажать пользователю.

---

## License

MIT
```
